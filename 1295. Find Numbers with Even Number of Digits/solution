class Solution {
    public int findNumbers(int[] nums) {
        int count=0,n=nums.length;;
        for(int i=0;i<n;i++){
            if(((int)Math.log10(nums[i])+1)%2==0){
                count++;
            }
        }
        return count;
    }
}